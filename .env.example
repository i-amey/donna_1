# cp .env.example .env and fill in. Never commit .env.
OPENAI_API_KEY=sk-...

# Override if OpenAI moves the endpoint or renames the model.
REALTIME_MODEL=gpt-realtime
REALTIME_SESSION_URL=https://api.openai.com/v1/realtime/sessions

HERMES_BIN=/home/ubuntu/.local/bin/hermes
HERMES_TIMEOUT=120
